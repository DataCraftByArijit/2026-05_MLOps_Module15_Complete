cd session_2_streamlit

streamlit hello

streamlit run stock_market.py

pip freeze > requirements.txt   (All packages in venv)
#OR
pipreqs .    (Only the required packages)

-------------------------------------

git pull

git add .

git commit -m "adding file for stock_market streamlit"

git push


